[theme]
# Tema nativo de Streamlit — reemplaza ~250 líneas de CSS inyectado.
# Al definirlo aquí, el motor de temas de Streamlit genera los estilos
# correctos para TODOS los widgets (inputs, selectbox, multiselect,
# date_input, etc.) sin necesidad de pelear con !important ni JavaScript.
base = "light"
# Rojo de accion: vivo (saturacion 100 %) y legible -- 5,48:1 con letra
# blanca, por encima del 4,5:1 que pide la norma. El #E57373 anterior se
# quedaba en 2,99:1, medido en el navegador sobre los botones reales.
primaryColor = "#D50000"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F2F2F2"
textColor = "#111111"
font = "sans serif"

# El tamano base se queda en el que trae Streamlit (16 px). Se probo
# subirlo a 18 y la aplicacion entera se veia como si tuviera zoom: para
# trabajar comodo tocaba alejar la pagina con el navegador, que es peor
# remedio que la enfermedad. Los tamanos del CSS quedaron en rem, asi que
# si algun dia se quiere volver a tocar, basta con esta linea.

[client]
# Antes: true. Cualquier error mostraba la traza de Python completa a
# quien estuviera usando la app -- rutas del servidor, nombres de tabla y
# el fragmento de consulta que falló. El personal no puede hacer nada con
# eso y a un extraño le sirve de mapa. Los errores siguen apareciendo en
# los logs de Streamlit Cloud, que es donde hay que mirarlos.
showErrorDetails = false
toolbarMode = "minimal"

[server]
enableStaticServing = true
